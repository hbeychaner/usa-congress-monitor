# Historical snapshot from a one-time pip -> uv migration (script since removed).
# pyproject.toml + uv.lock are authoritative; this file is not read by `uv sync`.
packages:
  - bs4==0.0.2
  - openai==1.61.1
  - python-dotenv==1.0.1
  - elasticsearch==9.3.0
  - pytest==9.0.2
  - pytest-asyncio==1.3.0